namespace OSFramework.Enum {
    /**
     * Internal type definition
     */
    export enum OS_Config_Map {
        advancedFormat,
        extendedClass,
        height,
        center,
        offset,
        showTraffic,
        staticMap,
        style,
        type,
        uniqueId,
        zoom
    }
}

