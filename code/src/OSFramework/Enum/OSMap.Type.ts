namespace OSFramework.Enum.OSMap {
    /**
     * Internal type definition
     */
    export enum Type {
        Roadmap = 'Roadmap',
        Satellite = 'Satellite',
        Hybrid = 'Hybrid',
        Terrain = 'Terrain'
    }
}
