// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace OSFramework.Maps.Enum {
	/**
	 * Internal type definition
	 */
	export enum OS_Config_FileLayer {
		layerUrl,
		preserveViewport,
		suppressPopups,
	}
}
