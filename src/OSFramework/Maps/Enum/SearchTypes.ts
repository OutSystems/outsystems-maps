// eslint-disable-next-line @typescript-eslint/no-unused-vars
namespace OSFramework.Maps.Enum {
	export const enum GoogleSearchPlacesApiVersion {
		v1 = 'v1',
		v2 = 'v2',
	}

	/**
	 * Internal type definition
	 */
	export enum SearchTypes {
		Addresses,
		Cities,
		Establishments,
		Geocodes,
		Regions,
	}
}
